<%= form_for @user, :url => url_for(:controller => 'sessions', :action => 'create') do |f| %>
  <%= f.label :name %>
  <%= f.collection_select(:id, User.all, :id, :user_name) %>
  <%= f.submit "Sign In" %>
<% end %>