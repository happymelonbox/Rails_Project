<%= link_to "Add Note", new_note_path(receipt_id: @receipt.id) %>
<%= render partial: "notes/notes", locals: {notes: @receipt.notes} %>
<%= render partial: "notes/form", locals: {note: @receipt.notes.build} %>
